name: Deploy and Test

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run lint and typecheck
      run: |
        npm run lint
        npm run typecheck

    - name: Build project
      run: npm run build

    - name: Deploy to Vercel
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-args: '--prod'
        vercel-org-id: ${{ secrets.ORG_ID }}
        vercel-project-id: ${{ secrets.PROJECT_ID }}
        working-directory: ./

  post-deploy-test:
    runs-on: ubuntu-latest
    needs: deploy
    if: success()

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright browsers
      run: npx playwright install --with-deps chromium

    - name: Run post-deploy performance tests
      run: |
        # 修改playwright配置使用生产环境URL
        echo "Testing production deployment..."

        # 这里可以配置生产环境的URL进行测试
        # npx playwright test --config=playwright.prod.config.ts

    - name: Run Lighthouse on production
      run: |
        echo "Running Lighthouse on production..."
        # 这里可以配置针对生产环境的Lighthouse测试
        # npx lhci autorun --config=lighthouserc.prod.json